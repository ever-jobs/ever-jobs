# Tasks: 1713 — ZipRecruiter: region guard, geo-block diagnostics and the jobs-app response contract

- [x] T1 — Region guard before any client or request. Acceptance: Germany and UK return `[]` + `bad_input` naming US/Canada with `createHttpClient`, GET and POST never called; USA, CANADA, US_CANADA, WORLDWIDE and an unset country search; `ZIPRECRUITER_REGION_GUARD=false` searches Germany.
- [x] T2 — `isGeoBlockError` + `describeError`. Acceptance: a 403 with `error_code` containing `cf-waf` (object, JSON string or plain text body) is `blocked` with a detail naming `cf-waf` and the North-America-only rule (review fixup: no proxy advice); a plain 403 is `blocked` without it; a 401 carrying the body is not a geo-block; 429 is `fetch_error`.
- [x] T3 — Geo-block memo. Acceptance: inside 30 min the same egress makes zero requests and says "not retried until"; a proxied request still runs; after the TTL the direct egress runs again; a geo-blocked session also starts it; `ZIPRECRUITER_GEO_BLOCK_TTL_MS=0` turns it off; the map never holds more than 64 egresses and evicts the oldest.
- [x] T4 — Session event. Acceptance: form-encoded body with `event_type=session`, `logged_in=false`, repeated `property` entries including a generated timestamp, `Content-Type: application/x-www-form-urlencoded`; client created with `cookies: true` and the caller's `proxies` / `requestTimeout`; property keys are the pre-1713 ones plus `locale` and `timestamp`; a `cf-waf` session skips the GET; any other failure only warns.
- [x] T5 — Query parameters. Acceptance: `radius`, `days` (36 h → 2, 3 h → 1), `remote=1`, `zipapply=1`, `employment_type=full_time`; none of `radius_miles` / `days_ago` / `form` / `continue_token`; `employment_type` omitted for an unmapped type; `ZIPRECRUITER_LEGACY_PARAMS=true` sends the pre-1713 query and JSON session body byte for byte and follows `continue_token`.
- [x] T6 — Pagination loop. Acceptance: `continue` → `continue_from` on page 2; one `randomSleep(5000, 10000)` between two pages, none before the first; a repeated-id page with a token stops at ≤ 2 GETs; an empty page stops; 1000 wanted → 10 GETs; 15 wanted → 2 GETs; `ZIPRECRUITER_MAX_PAGES=3` → 3 GETs; `offset: 1, resultsWanted: 1` returns the second job; `resultsWanted: 0` makes no request.
- [x] T7 — Field mapping. Acceptance: `zr-<listing_key>`; double-slash `jobUrl`; `{120000, 150000, YEARLY, USD}` and `{18.5, 21, HOURLY, CAD}`; `datePosted` `2026-09-20` and `datePostedAt` `2026-09-20T23:10:00.000Z`; `listingType`; `FULL_TIME` / `PART_TIME` / `CONTRACT` (`contractor`); Toronto, ON is Canada with `countryCode: 'CA'`; a boolean or `'TRUE'` `remote` is kept as remote; `remote=1` searches mark every job remote; markdown / plain / html descriptions; `jobUrlDirect` from `apply_url` or a `job_url` parameter; retired `job_id` / `id` / `job_url` / `url` / `salary_*_annual` fallbacks; a record with no link is dropped; currency defaults by country; `per hour` → `HOURLY`; zero pay → no compensation.
- [x] T8 — `hoursOld` client filter. Acceptance: with the clock at 2026-09-24T12:00Z and `hoursOld: 36`, the 09-20 job is dropped and the 09-23 and undated jobs kept; `ZIPRECRUITER_HOURS_FILTER=false` keeps both dated jobs.
- [x] T9 — No silent zero. Acceptance: page 2 failing with 500 returns page 1's jobs plus `fetch_error`; a page of id-less records returns `unknown` naming `listing_key`; a challenge page is `blocked`; another non-JSON body is `unknown`.
- [x] T10 — Unit suite `ziprecruiter.service.spec.ts` with synthetic fixtures. Acceptance: 54/54 pass; against the pre-1713 service 44 of 54 fail.
- [x] T11 — E2E. Acceptance: an ungated offline case (Germany → `bad_input` in under 1 s); the live case is gated on `RUN_NETWORK_E2E`, fails on a bare zero, checks `zr-` ids and the double-slash link, and expects `blocked` + `cf-waf` from a non-NA egress. 1 passed, 1 skipped without the gate.
- [x] T12 — `@SourcePlugin` description and the header-identity comment in the constants file. Acceptance: headers unchanged; no header, user-agent or device value added.
- [ ] T13 — Verification from a US/CA egress (spec section 9, Q1-Q4) and a trimmed real page to replace the synthetic fixtures. Owner.
- [ ] T14 — Ops: disable `zip_recruiter` for the EU fleet or give it a North-American proxy (spec section 9, Q6). Live change; needs a board claim.
- [x] T15 — Docs: `docs/index.md` row, `docs/log.md` entry, `docs/questions.md` for Q1-Q6. Integrator.

Integration 2026-09-25: `docs/index.md` / `docs/log.md` rows. Q1-Q6 stay in spec section 9; `docs/questions.md` Q-099 records that the plugin requests robots-disallowed paths.
