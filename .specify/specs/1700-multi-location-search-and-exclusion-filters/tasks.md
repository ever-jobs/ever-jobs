# Tasks: 1700 — Multi-location search and exclusion filters

- [x] T1 — `ScraperInputDto`: `locations`, `excludeTitleTerms`, `excludeKeywords`, `excludePresets` with class-validator metadata; `HARD_MAX_SEARCH_LOCATIONS` (25), `DEFAULT_MAX_SEARCH_LOCATIONS` (10), `MAX_SEARCH_LOCATION_LENGTH` (200), `MAX_EXCLUSION_TERMS` (50), `MAX_EXCLUSION_TERM_LENGTH` (100); `ExclusionPreset` enum. Acceptance: fields survive `whitelist: true`, bad values fail validation, no constructor defaults — `scraper-input-search-filters.spec.ts` 9/9.
- [x] T2 — `search-locations.ts`: normalise, resolve (`location` first, case-insensitive de-dup, diacritics kept, cap + `overCap`), order-insensitive cache key, `clampMaxLocations`. Acceptance: `search-locations.spec.ts` 25/25.
- [x] T3 — `job-exclusion.ts`: linear entity decode + tag strip, NFKD fold, clause-aware tokenizer, aliases, whole-token/phrase/prefix terms, same-clause negation, separator-less scripts, `security_clearance` preset, metrics, `exclusionSpecFromInput` / `hasExclusionInput`. Acceptance: every P1–P8 pitfall, the 40-title preset fixture and both timing guards — `job-exclusion.spec.ts` 59/59.
- [x] T4 — `JobsService` multi-location: `planLocations` (absent → untouched; 0/1 → legacy path), `scrapeSiteAcrossLocations` (sequential per site, fresh clone per location, deadline per location, polite stop on 429/blocked/breaker, pause between attempted calls), `mergeLocationOutcomes` (site + id + jobUrl identity de-dup, `LocatedSourceDiagnosticDto` rows), over-cap `location:` rows, `EVER_JOBS_SEARCH_MAX_LOCATIONS` / `EVER_JOBS_SEARCH_LOCATION_INTERVAL_MS`. Acceptance: `jobs.service.multi-location.spec.ts` 28/28 (a mutation disabling the de-dup and the stop fails 10 of them); existing `jobs.service.spec.ts` unchanged and green.
- [x] T5 — `JobsAggregator` exclusions: pass-through paths filter rows, dedup path drops whole clusters, persistence untouched, zeroed metrics on the empty/inactive paths, `ERR_EXCLUSION_FAILED` degrades to the unfiltered list, no new keys when the option is absent. Acceptance: `jobs.aggregator.exclusions.spec.ts` 12/12; existing aggregator suites green.
- [x] T6 — `search-cache-params.ts` + controller + resolver + GraphQL: exclusions never keyed, `locations` keyed order/case-insensitively on the searched set, `exclusion_metrics` (samples only with `?diagnostics`), post-exclusion `count`/`total_pages`, liveness only on kept jobs, `/analyze` filtered, GraphQL fields validated and `exclusionMetrics` nullable. Acceptance: `search-filters.api.spec.ts` 34/34; existing controller/resolver/schema suites green.
- [x] T7 — CLI `--locations`, `--exclude-title`, `--exclude-keyword`, `--exclude-preset` on `search` and `compare`; stdout stays pure JSON, the excluded count goes to stderr, `--stdin` JSON filtered too. Acceptance: `search-filters.command.spec.ts` 10/10 (parsed with nest-commander's own commander build).
- [x] T8 — MCP `search_jobs`: `locations`, `exclude_title_terms`, `exclude_keywords`, `exclude_presets` in the schema and body (only when non-empty, all key styles), unknown presets dropped, `excluded` in the response only when filtering. Acceptance: `tools-search-filters.spec.ts` 13/13; existing MCP suites green.
- [x] T9 — `tool_manifest.json`: four input properties, two features, two examples.
- [x] T10 — Live check `apps/api/__tests__/search-multi-location.e2e-spec.ts` (moved there in the review fixup so the CI `apps/api/__tests__/(search|analyze)` job runs it): two upstream GETs 500 ms apart; one row per location; no same-source duplicate (40 raw → 7 removed on the run of 2026-09-25); exclusion invariants on the same live titles.
- [x] T11 — `tsc --project tsconfig.typecheck.json --noEmit` clean.
- [x] T12a — (integrator) `docs/log.md`, `docs/index.md`, `docs/API_CHANGELOG.md`, `docs/CLI.md`, `README.md` parameter/CLI tables. Q-A...Q-E stay in this spec (the integration adds Q-099).
- [x] T12b — `docs/PERFORMANCE_TUNING.md` "Multi-location search" section: locations × sources, the cap, `EVER_JOBS_SEARCH_LOCATION_INTERVAL_MS`, plugin request gaps, the response memo, the deadline (review fixup).
- [x] T13 — Scoped response memo in the shared HTTP client (`packages/common/src/http/http-memo.ts`, review fixup): each source's location loop runs in `runWithHttpMemo`, so a repeated identical GET or POST is answered from the first answer and N locations cost one board fetch for local-filter plugins. `EVER_JOBS_SEARCH_LOCATION_MEMO=off` / `get` restores or narrows it. Tests: `http-memo.spec.ts` (real local server), memo cases in `jobs.service.multi-location.spec.ts`. The default pause was NOT dropped to 0: sources that send the location to their host still make one real request per location, and plugins outside the shared client get no memo.
- [x] T14 — Plugin request gaps (review fixup): `IPluginMetadata.minRequestIntervalMs`, declared by LinkedIn / Wellfound / Naukri (3 s) and Glassdoor / ZipRecruiter (5 s); the location pause is the larger of it and the operator interval.

Integration 2026-09-25: T12a by the integrator; T12b, T13 and T14 in the review fixup.
