# Tasks — Spec 1736: Workday Company Sources

- [x] T1 — Look up tenant, cluster and site for the candidate employers.
- [x] T2 — Verify each board live (Spec 1735 probe; 2026-09-24).
- [x] T3 — Seed the 53 verified companies (naming, domains, tags, boards).
- [x] T4 — Scaffold the 53 plugins and wire them at the tail of the four shared files.
- [x] T5 — Generated suites green against the real Workday adapter; `tsc` clean.
- [x] T6 — Follow-up (Q-107, review 2026-09-25): pass the trimmed `searchTerm` to Workday's `searchText` in the adapter (`''` in list mode) so keyword searches stop enriching every posting of every tenant.
- [ ] T7 — Follow-up (Q-109): cover the listed non-Workday employers through their own ATS adapters (Oracle HCM for Dell, etc.).
- [x] T8 — Review follow-up (Spec 1735 §4.6): Workday detail enrichment sequential — 1 request in flight, 250–500 ms before each detail request; adapter suite asserts at most one in flight.
- [x] T9 — Review follow-up (Spec 1735 §4.2.1): keep a posting's Workday business-unit name; re-stamp only empty, tenant-token and legal-form names. **Superseded by T13.**
- [x] T10 — Merge/deploy gate (§7): deploy only after the ever-hust full-result consumer is live, or ship with the batch listed in `EVER_JOBS_DISABLED_SOURCES` until it is. **Withdrawn by T16** (owner decision: ship enabled).
- [x] T11 — Review follow-up F8 (spec §8): bound one Workday scrape — at most `WORKDAY_MAX_DETAIL_FETCHES` (default 50) detail requests, the rest returned at list level; `WORKDAY_SCRAPE_TIME_BUDGET_MS` (default 90 s, `0` = off) over listing and enrichment, a budget spent while listing reported as `partial`; list-level postings keep the enriched id (row requisition id) and link through the career site. Adapter suite red against the pre-T11 adapter first, then the change.
- [x] T12 — Review round 2, R3 (spec §8.1): an `additionalLocations` entry is a site only when it has a location shape (`hasWorkdayLocationShape`: the shared parser finds remote work, a state or a country, or a part is a US state or UK nation); a rejected entry (Moderna: "Drug Manufacturing") becomes the department when there is none. Shapeless entries are kept when the primary is itself shapeless. Tested with the recorded Moderna detail.
- [x] T13 — Review round 2, S2/R2 (spec §8.1): one identity per posting whether enriched or returned at list level. Company name: the adapter names every posting by its tenant (the detail-only `hiringOrganization` made a posting switch between a business unit and the display name as it crossed the detail cap) and the generator re-stamps the display name for Workday like every backend (re-scaffold of the 55 Workday plugins; each suite runs the real adapter with one detail request and a business-unit detail). Location: the row label falls back to the first `bulletFields` entry that is not the requisition id and has a location shape when `locationsText` is missing (Moderna); with no requisition country a single site in a US state (50 + DC) takes `United States`, as the Spec 1689 overlay does for a US requisition. `datePosted` stays absolute (detail `startDate`, else the row label resolved at scrape time). Verified with the recorded Moderna page + detail: same id, title, company, location and dedup key at both levels. Consumers are told to key Workday postings on `id` (spec §8.1, `docs/DEPLOYMENT.md`, `docs/ATS_INTEGRATIONS.md`).
- [x] T14 — Review round 2, S5 (spec §8, "Ids"): a `bulletFields` entry is taken as the list-level requisition id only when it appears in `externalPath` as a whole token (case-insensitive; not inside a longer token, not in the query string); otherwise the path's `_<id>` suffix. All 168 rows the Spec 1735 verifier recorded pass the check, so fixtures and ids are unchanged.
- [x] T15 — Review round 2, S3/S4 (spec §8.2): document that `WORKDAY_SCRAPE_TIME_BUDGET_MS` runs from each board's own scrape start, independent of the fan-out deadline, and must stay below the deadline the deployment sets; name the deadline `EVER_JOBS_FANOUT_DEADLINE_MS` (preferred) with `EVER_JOBS_SEARCH_DEADLINE_MS` as fallback in `docs/DEPLOYMENT.md`, `.env.example`, `workday.constants.ts` and this spec. Cheap deadline hint: the adapter reads the fan-out deadline from the same variables (same precedence and parsing as the API) and caps its budget at 3/4 of it (defaults unchanged: 90 s of 120 s); `0` budget stays off, a disabled deadline applies no cap; a listing cut short by the capped budget names the cap.
- [x] T16 — Review round 2, S1 — owner decision (2026-09-26, spec §7, Q-107): the new Workday and quant plugins ship ENABLED by default; the detail cap and the time budget (§8, §8.2) bound their cost. `docs/DEPLOYMENT.md` and `.env.example` describe the 55-token `EVER_JOBS_DISABLED_SOURCES` line as an optional emergency switch, not a required gate. No code change: nothing in the code gated the batch.
- [x] T18 — PR #99 review (2026-09-26): with no enriched posting dating the board, a relative label stays undated instead of counting from UTC's date (a day off for part of every day). Acceptance: the T17 no-startDate / no-detail cases give `null` for every relative row; an absolute `postedOn` is still parsed; red against the previous adapter.
- [x] T17 — Live retest finding (2026-09-26, spec §8.1 "Date — the board's calendar"): list-level `datePosted` was one day late on 20 of 20 Moderna postings at 00:42 UTC — Workday counts "Posted Today / Yesterday / N Days Ago" on the board's (tenant's) calendar and the adapter counted from the UTC date. The adapter dates the board from its enriched postings (row label + detail `startDate`, samples beyond ±1 day of UTC ignored, majority wins) and counts every relative label of the scrape from that date; with no enriched sample it keeps the UTC date. Tested with a recording of Moderna's first page and all 20 details at 01:33 UTC (fixed clocks either side of UTC midnight, a board ahead of UTC, the board's midnight during enrichment, a repost, the undated fallback) and in six real host time zones through a child process; red first on the unfixed adapter.
