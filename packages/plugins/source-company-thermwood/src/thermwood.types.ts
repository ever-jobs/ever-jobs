/** One parsed `div.job-card` from the careers page. */
export interface ThermwoodJobCard {
  title: string;
  dateText: string;
  locationText: string;
  typeText: string;
  descriptionHtml: string;
  descriptionText: string;
}
