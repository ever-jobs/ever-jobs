/** One open role parsed from the careers listing page. */
export interface CanekastOpening {
  /** Role title, e.g. "Foundry Operator". */
  title: string;
  /** Absolute URL of the role's PDF job description. */
  pdfUrl: string;
}
